-- TODO: Can I set these options from the command line?
set-pp-renderer ascii
set-pp-width 80
-- set-pp-type Show
set-pp-type Omit

try unshadow

try reify-module
try (repeat (any-td (repeat reify-misc)))

down                                    -- To definitions

-- Use this rule only we do all the reification we can,
-- in order to finish generating an EP.
-- 
--   any-td (apply-rule "reify/oops" ; cleanup-unfold)

try unshadow
