-- set-pp-type Show
reify-module
try (repeat (any-td reify-misc))
try (any-bu reify-rules) ; try simplify -- See that everything reifies
try unshadow                            -- useful?
down                                    -- To definitions
