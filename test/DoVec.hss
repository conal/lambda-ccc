set-pp-renderer ascii
set-pp-width 80
set-pp-type     Show
set-pp-coercion Abstract
-- set-pp-type     Omit
-- set-pp-coercion Omit

-- rhs-of z

-- try post-unfold

-- reify-module

binding-of main
try (any-td (unfold go))
try bash  -- for go unfolding

-- try (repeat (any-td (repeat reify-misc)))
-- try bash

try unshadow

-- occurrence-of reifyEP ; up ; up
-- reify-case-vec

-- set-pp-type Omit

-- down


-- -- Remove when I'm able to reactiveate -dcore-lint

-- top ; lint-module ; binding-of main


-- Remove later:

-- occurrence-of reifyEP ; up ; up
-- set-pp-type Show
-- set-pp-coercion Show
-- -- reify-vecs

