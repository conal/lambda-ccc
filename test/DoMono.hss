set-pp-renderer ascii
set-pp-width 80

set-pp-type     Show
set-pp-coercion Kind

-- set-pp-type Omit
-- set-pp-coercion Omit
-- set-pp-coercion Abstract

rhs-of 'test
try (repeat passE)
try simplifyAll'
-- I tried moving standardizeCase and standardizeCon into mySimplifiers (used by
-- simplifyAll'), and it appears to loop. To do: investigate. Meanwhile, do it here.
try (any-td (repeat (standardizeCase <+ standardizeCon)))
try simplifyAll'
try unshadow

