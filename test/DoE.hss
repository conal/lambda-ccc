set-pp-renderer ascii
set-pp-width 80
set-pp-type     Show
set-pp-coercion Show
-- set-pp-type     Omit
-- set-pp-coercion Omit

rhs-of e4
-- repeat (repeat (any-td (repeat inline)) >>> try bash)

-- unfold sum
-- any-td eta-reduce  -- \ x -> g x
-- let-subst

-- occurrence-of "$fEncodablePair"
